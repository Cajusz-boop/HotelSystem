# =============================================================================
# Synchronizacja Bistro → HotelSystem (karta dań + rachunki na pokój)
# =============================================================================
# Skopiuj do .env w tym samym folderze (bistro-sync/.env)

# Baza Bistro (KW Hotel / Symplex)
BISTRO_DATABASE_URL=mysql://admin:haslo@10.119.169.20:3306/kwhotel

# API HotelSystem — synchronizacja karty dań
MENU_SYNC_URL=https://hotel.karczma-labedz.pl/api/v1/external/menu-sync

# Klucz API (taki sam jak EXTERNAL_API_KEY w .env aplikacji)
EXTERNAL_API_KEY=twoj-klucz-api

# Opcjonalnie: interwał synchronizacji menu (minuty) gdy uruchamiasz bez --once
# SYNC_INTERVAL_MINUTES=5
