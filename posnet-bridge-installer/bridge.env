# Konfiguracja POSNET Trio Bridge
# Skopiuj ten plik jako .env do folderu bridge'a lub ustaw zmienne systemowe.

# Tryb pracy bridge'a:
#   tcp   = komunikacja z prawdziwą drukarką POSNET (PRODUKCJA)
#   spool = zapis do plików JSON (TESTY)
POSNET_BRIDGE_MODE=tcp

# Adres IP drukarki POSNET Trio (sprawdź w ustawieniach drukarki)
POSNET_PRINTER_HOST=10.119.169.55

# Port TCP drukarki (domyślnie 6666 dla POSNET Online)
POSNET_PRINTER_PORT=6666

# Port HTTP bridge'a (nie zmieniaj bez potrzeby)
POSNET_BRIDGE_PORT=9977

# Timeout połączenia z drukarką w ms
POSNET_PRINTER_TIMEOUT=5000
